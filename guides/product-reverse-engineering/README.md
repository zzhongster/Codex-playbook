# 跨技术栈产品逆向工程指南

## 目的

本指南帮助团队把遗留产品的界面、代码、数据与运行行为还原为可追溯、可验证、可交付的产品知识。重点不是逐文件翻译，而是建立“业务问题—运行证据—跨栈实现—结论—验证”的证据链，让项目负责人能控制范围与风险，让执行团队能按同一套门槛协作。

这是一个 Guide，用于组织工作流与证据，而不是一个会因结构校验通过就自动获得验证状态的 Pattern。可复用结论只有满足贡献规范中的独立证据要求后，才能晋升为 Pattern、Anti-pattern 或 Experiment。

## 适用受众

### 给项目负责人

使用本指南确定逆向目标、优先级、完成定义和安全边界；根据证据成熟度判断哪些结论可以用于产品决策，哪些仍需取证。先走下方五分钟选型流程，再按发布顺序分配阶段负责人。

### 给执行团队与 AI Agent

使用本指南统一采集证据、追踪跨技术栈调用链、区分观察与推断，并在发布前执行同一个验证入口。任何自动化都必须服从数据授权、最小访问和人工复核边界。

## 五分钟选型流程

**证据成熟度：`proposed`** — 本节的选型步骤和任务说明是待跨项目验证的工作流建议，不代表已经形成 Pattern。

| 时间 | 判断 | 选择 |
| --- | --- | --- |
| 0–1 分钟 | 目标是回答“产品做什么”还是“代码怎么做”？ | 前者以业务流程和行为证据为主，后者补充代码与数据追踪；两者都要写明决策用途 |
| 1–2 分钟 | 是否能安全运行原系统？ | 能运行则设计最小可重复场景；不能运行则标注限制，只做静态证据链，不猜测运行结果 |
| 2–3 分钟 | 问题是否跨越界面、服务和数据层？ | 跨层问题使用端到端追踪；单层问题仍记录上下游假设，避免局部结论越界 |
| 3–4 分钟 | 当前证据属于哪种成熟度？ | 从下方四个标签中选择一个；不确定时使用较低成熟度 |
| 4–5 分钟 | 交付物面向决策还是执行？ | 决策交付突出范围、风险和置信度；执行交付补齐定位、复现步骤与验证命令 |

选型结束时应得到一条短任务说明：目标问题、允许的数据源、运行权限、目标成熟度、负责人和完成定义。缺少其中任何一项，先补齐边界再取证。

## 证据成熟度图例

| 标签 | 在本指南中的含义 |
| --- | --- |
| `cross-project-validated` | 已由至少两个相互独立的项目验证，可申请提炼为 Pattern，但仍需逐项审查证据与边界 |
| `project-validated` | 已在一个真实项目验证，只能声称该项目范围内成立 |
| `industry-established` | 有可追溯的行业标准、权威规范或稳定公开实践支持，仍需确认版本和本地适用性 |
| `proposed` | 待验证的工作假设或候选做法，必须明确标为建议，不能冒充观察结果 |

## 当前范围

当前版本建立指南入口、治理规则、受众分流、证据成熟度和发布顺序，并纳入目标与范围、安全边界、证据模型及统一术语四项基础契约。它适用于从桌面端、Web、移动端到服务端和数据库的跨技术栈产品逆向，也适用于仅能静态阅读的遗留系统。后续阶段手册必须引用这些基础契约；未纳入导航的内容不应被视为本指南的已发布模块。

不在当前范围内：绕过访问控制、获取未授权数据、替代法律或合规审查，以及仅凭静态文本声称运行时行为已经成立。

## 安全边界

- 只分析已获授权的系统、代码、账号、环境和数据，遵守组织的数据分级与保留规则。
- 优先使用脱敏样本和最小权限；不得把密钥、令牌、个人信息、客户数据或本机绝对用户目录写入仓库。
- 运行遗留系统、迁移数据或调用外部服务前，明确副作用、隔离环境和恢复方案；不在生产环境做破坏性探测。
- 将事实观察、源码解释和推断分开记录。无法复现的行为必须降级成熟度并注明缺口。
- AI Agent 可以协助检索、关联和起草，但证据归属、风险接受和最终发布由人类负责人复核。

## 基础导航

| 文档 | 使用时机 |
| --- | --- |
| [目标、范围与完成定义](core/goals-scope-and-completion.md) | 立项时区分决策目的、固定八个范围维度，并建立可计算的完成门槛 |
| [授权、隐私与安全](core/authorization-privacy-and-safety.md) | 任何取证前执行 G0，确定允许来源、隔离方式、保留期限和禁止事项 |
| [证据与置信度](core/evidence-and-confidence.md) | 登记证据、给主张定状态、处理冲突并建立跨对象追踪关系 |
| [术语表](glossary.md) | 项目成员对对象类型、相近概念或评审用语有歧义时查阅 |

## 导航与发布顺序

**证据成熟度：`proposed`** — 本节的发布顺序是当前建议，后续应根据真实项目证据调整并提升成熟度。

指南按以下顺序发布和使用；后一步引用前一步的边界与证据，不得跳过安全和验证门槛：

1. **入口与选型**：本页，确定受众、范围、权限、成熟度和完成定义。
2. **任务简报与边界**：使用[目标、范围与完成定义](core/goals-scope-and-completion.md)、[授权、隐私与安全](core/authorization-privacy-and-safety.md)和[术语表](glossary.md)，固定业务问题、系统范围、术语、数据授权和禁止事项。
3. **证据清单与可追溯性**：按[证据与置信度](core/evidence-and-confidence.md)登记界面、运行记录、源码、数据库、文档及其来源、状态与置信度。
4. **跨栈追踪与行为重建**：从用户动作追到客户端、服务、数据和外部依赖，形成可复现的业务叙事。
5. **差异验证与发布**：用对照场景审查观察与推断，标注成熟度，并按贡献规范提炼正式条目。

当前入口、治理基线和四项基础契约纳入仓库验证。新增模块发布时，应加入本节导航并复用同一证据标签，不得另建平行成熟度体系。

## 验证

在仓库根目录运行唯一的指南结构验证命令：

```bash
python3 tools/validate_product_reverse_engineering_guide.py
```

该命令只检查指南入口与治理契约是否齐全；它不证明指南中的技术结论已经在项目中验证。
